{
  "settings": {
    "log_level": "info",
    "http_timeout": "30s"
  },
  "providers": [
    {
      "provider": "cloudflare",
      "domain": "${LOCAL_DOMAIN}",
      "host": "@",
      "token": "replace-with-cloudflare-token"
    }
  ]
}
